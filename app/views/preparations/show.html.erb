<p style="color: green"><%= notice %></p>

<%= render @preparation %>

<div>
  <%= link_to "Edit this preparation", edit_preparation_path(@preparation) %> |
  <%= link_to "Back to preparations", preparations_path %>

  <%= button_to "Destroy this preparation", @preparation, method: :delete %>
</div>
