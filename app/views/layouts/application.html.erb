<!DOCTYPE html>
<html lang="en">
  <head>
    <title>BiorepositoryPortal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css", rel: "stylesheet" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <a class="skiplink" href="#maincontent">Skip to main content</a>
    <% if controller.controller_name == 'items' && controller.action_name == 'search' %>
      <a class="skiplink" href="#search-results">Skip to search results</a>
    <% end %>
    <%= render partial: "layouts/navbar" %>
    <%= render partial: "layouts/additional_bar" %>
    <%= render partial: "layouts/number_of_items_to_loan" %>

    <main id="maincontent">
      <div id="flash">
        <%= render "layouts/flash" %>
      </div>
      <div class="<%= (controller.controller_name == 'home' && controller.action_name == 'about') || 
                  (controller.controller_name == 'sessions' && controller.action_name == 'new') || 
                  (controller.controller_name == 'collections' && ['show', 'index'].include?(controller.action_name)) ||
                  (controller.controller_name == 'items' && controller.action_name == 'search') ? '' : 'mx-9' %>">
        <%= yield %>
        <%= turbo_frame_tag "remote_modal", target: "_top" %>
      </div>
    </main>
    <%= render partial: "layouts/footer" %>
  </body>
</html>
