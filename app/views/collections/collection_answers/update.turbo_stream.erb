<turbo-stream action="replace" target="collection_question_<%= @question.id %>">
  <template>
    <%= render partial: "profiles/user_collections_answer", formats: [:html], locals: { collection: @collection, question: @question, answer: @answer } %>
  </template>
</turbo-stream>
