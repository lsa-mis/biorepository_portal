<div class="d-flex flex-row align-items-center justify-content-between">
  <h1>Information Requests Report</h1>
  <%= link_to 'Back to Reports', reports_path, class: "link_to" %>
</div>
<p>This report shows information on Information Requests.</p>

<div class="py-3">
  <%= render 'filter_form', report_url: information_requests_report_reports_path %>

  <% if params[:commit] && params[:format] == 'html' %>
    <%= render 'reports_table' %>
  <% end %>
</div>
