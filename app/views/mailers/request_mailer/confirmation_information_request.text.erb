Dear <%= @user.first_name %>,

We've received your loan request submitted on <%= @information_request.created_at.strftime("%B %d, %Y") %>
with the following question:

<%= raw(@message) %>
<% if @checkout_items.present? %>
 Checkout Items:
  <% @checkout_items.each do |item| %>
    <%= item %> <br>
  <% end %>
<% end %>

Thank you for using our system. We'll reach out to you if we need more information.

The Collections Team
